version: 2
ethernets:
  ens3:
     dhcp4: false
     addresses: [ ${ip_address}/${subnet_mask} ]
     gateway4: ${gateway_ip}
     nameservers:
       addresses: [ ${name_servers} ]
       search: [ ${domain} ]
